import 'package:flutter/material.dart';

void main() {
  runApp(BebekChat());
}

class BebekChat extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(title: Text('Bebek Chat')),
        body: Center(child: Text('Halo dari Bebek Chat!')),
      ),
    );
  }

