name: bebek_chat
description: A simple chat UI like WhatsApp
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_blue: ^0.8.0  # Tambahkan ini

flutter:
  uses-material-design: true
